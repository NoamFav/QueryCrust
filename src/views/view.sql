-- This is a placeholder SQL script for MySQL
-- It's intended to create a simple table to ensure this directory is tracked by Git.

CREATE TABLE IF NOT EXISTS placeholder_table (
    id INT AUTO_INCREMENT PRIMARY KEY,
    placeholder_column VARCHAR(255) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB;

-- This table is not intended for production use. It is merely a placeholder.*.log
*.sql
config/*
.example
